#include <iostream>

using namespace std;

int main(void)
{
    int l, b, a;
    cout << "Enter length: ";
    cin >> l;
    cout << "Enter breadth: ";
    cin >> b;
    a = l * b;
    cout << "The area is " << a << " sq. units." << endl;
    return 0;
}